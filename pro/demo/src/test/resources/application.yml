spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MariaDB;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  data:
    redis:
      host: localhost
      port: 6370   # 테스트에선 Redis 미사용 → 연결 실패해도 무방

jwt:
  secret: test-secret-key-for-unit-tests-minimum-256bit-length-padding

ai:
  server:
    url: http://localhost:8000
  service:
    mock: true
